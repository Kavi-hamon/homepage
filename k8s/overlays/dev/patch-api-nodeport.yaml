# Exposes the Go API on a fixed node port.
# Open: http://$(minikube ip):30808
apiVersion: v1
kind: Service
metadata:
  name: api
spec:
  type: NodePort
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30808
